#Solution to navigate the Maze in Reeborg's World.  Solution created using functions, while loops, and if statements. 
#The robot can be placed at any point in the maze, and it will still reach the goal.
#https://reeborg.ca/reeborg.html?lang=en&mode=python&menu=worlds%2Fmenus%2Freeborg_intro_en.json&name=Maze&url=worlds%2Ftutorial_en%2Fmaze1.json

#Define turn_right function
def turn_right():
    turn_left()
    turn_left()
    turn_left()

#Define while loop
while not at_goal():
    if right_is_clear():
        turn_right()
        move()
        if front_is_clear():
            move()
    elif wall_on_right():
        if wall_in_front():
            turn_left()
            if wall_in_front():
                turn_left()
                move()
            else: 
                move()   
